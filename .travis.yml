language: python
python:
  - "3.6.3"
# command to install dependencies
install:
  - pip install chardet
  - pip install flake8
  - pip install pydocstyle
  - pip install logbook
# command to run tests
script:
  # - python EricPackagesChecker.py
  - python "tests/eric_languagetool.py"
  - flake8 .