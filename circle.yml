machine:
  python:
    version: 3.6.3

dependencies:
  post:
    - pip install chardet
    - pip install clize
    - pip install colorama
    - pip install logbook
    - pip install pyfiglet
    - pip install termcolor

test:
  override:
    - python "tests/eric_run_tests.py"