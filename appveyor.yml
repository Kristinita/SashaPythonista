environment:

  matrix:

    - PYTHON: "C:\\Python36-x64"
      PYTHON_VERSION: "3.6.3"
      PYTHON_ARCH: "64"
      PIP: "C:\\Python36-x64\\Scripts\\pip"

platform: x64

install:

  - cmd: "%PIP% install --disable-pip-version-check --user --upgrade pip"
  - cmd: "%PIP% install chardet"

build_script:

  - cmd: "set PYTHONIOENCODING=utf-8"
  - cmd: "%PYTHON%\\python blabla.py"